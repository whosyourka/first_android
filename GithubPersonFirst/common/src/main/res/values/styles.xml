<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AppTheme.Base" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoTitle">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.45</item>
        <item name="android:windowFullscreen">true</item>
    </style>


    <!-- button. -->
    <style name="btn_submit_data_error"><!--刷新按钮-->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginLeft">25dp</item>
        <item name="android:layout_marginRight">25dp</item>
        <!--<item name="android:background">@drawable/blue_rounded_btn_bg</item>-->
        <!--<item name="android:textColor">@drawable/refresh_btn_textcolor</item>-->
    </style>


    <!-- 对话框 -->
    <style name="style_base_dialog" parent="@android:style/Theme.Dialog">
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 浮在界面上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 对话框背景 -->
        <item name="android:background">@android:color/transparent</item>
        <!-- 外部背景透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 对话框外的背景色：透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景是否模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!-- 加载对话框 -->
    <style name="loading_dialog" parent="@style/style_base_dialog">
    </style>
</resources>
